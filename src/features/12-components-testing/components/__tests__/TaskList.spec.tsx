import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';

// import TaskList, { Task } from '../TaskList';

// function renderTaskList() {
//   return;
// }

describe('TaskList', () => {
  it('should xxx', () => {
    const yyy = 123;
    expect(yyy).toEqual(123);
  });
});
