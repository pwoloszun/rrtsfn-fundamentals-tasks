import reducer, { actions, newSyncCounterSliceId } from './newSyncCounterSlice';

import * as selectors from "./selectors";

export {
  selectors,
  actions,
  newSyncCounterSliceId
};

export default reducer;
